/// Placeholder for the `emr serve` command (implemented in Story #5).
pub fn cmd_serve() {
    eprintln!("error: 'serve' is not yet implemented (planned for Story #5)");
    std::process::exit(1);
}
